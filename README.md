# SkyCompass Satellite (人造天体观测罗盘)

**简体中文** | [English](README_EN.md)

## 项目概述

SkyCompass Satellite 是 SkyCompass 项目的扩展演化版本，运行在 M5Stack Cardputer ADV (ESP32) 上。与原项目关注自然天体（太阳、月亮等）不同，Satellite 版本专注于**人造飞行器（如国际空间站 ISS、天宫空间站、哈勃望远镜等）**的实时轨道追踪与可视性预测。

**核心产品定位：** 向用户回答：**“近期有哪些人造飞行器值得观测？”**，它不仅是一个轨道展示工具，更是一个“今晚值得抬头看的飞行器推荐系统”。

## 硬件支持

- M5 Cardputer ADV（ESP32）
- 六轴IMU（加速度计 + 陀螺仪）
- 内置GNSS模块（用于定位及获取高精度 UTC 时间）
- 键盘 + 小尺寸屏幕

## 核心特性

- **全景快捷交互与按键映射**：
  - `Enter`：展开/收起近期最佳观测窗口推荐面板。
  - `S` 键：调出卫星复选菜单，自由决定同屏显示的目标。菜单内按 `d` 键可删除自定义卫星。
  - `W` 键：开启/关闭 WiFi 并进行 NTP 时间同步与轨道数据更新。
  - `H` 键：随时在屏幕中央悬浮呼出**快捷键帮助菜单**。
  - `C` 键：进入/退出**手动定位模式 (十字准星模式)**，使用 `;`(上) `.`(下) `,`(左) `/`(右) 键如同操纵摇杆般自由拨动地球，手动指定任意观测地点。
  - `G` 键：开启/关闭 **GNSS 强制常开模式**，退出系统自动休眠策略以手动获取定位。
  - `V` 键：进入/退出**飞行器跟随视角 (Sat View Mode)**，在此模式下可通过 `;`(上一个) 和 `.`(下一个) 键快速切换当前聚焦的卫星。
- **实时模式 (Live Mode) & 时光机 (Time Machine)**：
  - 纯代码绘制极简地球与**智能冷暖色渐变大陆轮廓**，动态渲染昼夜交替。
  - 具备**视觉锚点系统**：自动测算并在屏幕角落浮现“动态指北针”；在极点区域渲染“十字虚线与立体地轴”，彻底解决上帝视角下的方向迷失问题。
  - 按下 **`,` (后退)** 或 **`/` (快进)** 键可以呼出**时光机**，支持长按极速穿梭时间，直观洞察未来的过境轨迹。
- **观测推荐系统 (核心价值)**：
  - 在主界面按 **`Enter` 键** 可滑出左侧推荐面板。
  - 综合计算地球遮挡、太阳照明，结合亮度评分输出最佳观测窗口（AOS、最高仰角时间）。
- **设备端全闭环运行与离线生存能力**：
  - 支持按下 **`W` 键** 直接在设备屏幕上扫描周边热点并使用全键盘输入密码连接。
  - 连网后全自动完成 NTP 时间同步、Celestrak 最新 TLE 轨道根数下载（写入本地 LittleFS 闪存）以及 **IP 城市级快速定位**。
  - **离线秒开与 GNSS 同步**：无网环境下，开机秒速加载本地 LittleFS 闪存的轨道缓存；一旦在室外获得 GNSS 锁定，设备会自动解析原子钟 UTC 时间校准推演时间轴，并更新定位至真实坐标。
  - **智能刷新策略 (48小时阈值)**：TLE 数据有效期被设定为 48 小时。网络任务仅在缓存超过此阈值时才会重新下载，完美兼顾肉眼/望远镜观测对时效性的不同要求。
- **极致的电源管理**：
  - 网络任务处理完毕后，立刻自动切断 WiFi 射频以节省电量。
  - 对于 GNSS 定位模块，一旦成功获得 3D Fix 并在同步时空坐标后，或搜星超时（1分钟）未果，即向其下发休眠指令（`$PCAS10,0*1C`），大幅提升续航表现。

## 与 SkyCompass 的关系

**共享底层基础：**
- 时间系统与 GNSS 解析
- 坐标转换体系 (ECEF/LatLon/Alt -> Az/Alt)
- 底层 UI 绘制与渲染框架
- 天球计算与太阳位置模型

**业务差异化：**
| 模块   | SkyCompass 原版 | SkyCompass Satellite |
| ---- | ---------- | ---------- |
| 目标   | 太阳/月亮/银河等自然天体       | 人造卫星 / 空间站       |
| 轨道   | 缓慢周期变化        | 极高速变化 (约90分钟绕地球一圈)       |
| 核心体验 | 空间指向与 3D 沉浸观测         | 观测推荐与时间窗口规划       |
| 数据基座   | 天文历表公式       | TLE + SGP4 轨道传播模型 |

## 架构拆解与开发路径

SkyCompass Satellite 的架构已经按以下路径完成开发并成功落地：

- `[x]` **Phase 0 (架构验证)**：打通 SGP4 模型，实现 TLE 解析与空间坐标解算。
- `[x]` **Phase 1 (地球视图 MVP)**：完成极简地球绘制与坐标映射，在屏幕上闭环跑通环绕轨迹。
- `[x]` **Phase 2 (太阳与光照判断)**：融入昼夜圈逻辑，实现精确的地球阴影遮挡判断（可见性前提）。
- `[x]` **Phase 3 (观测推荐系统)**：基于规则引擎（仰角/亮度/时长）输出评分与推荐计划。
- `[x]` **Phase 4 (网络与数据基座)**：实现设备端 WiFi 扫描/连接、LittleFS 缓存、NTP 同步与 IP 城市粗定位。
- `[x]` **Phase 5 (交互升维)**：引入动态卫星复选列表、智能防重叠 UI 布局、自动休眠省电策略以及时光机漫游功能。

## 未来展望 (TODO List)

以下是未来计划或正在评估中的新功能特性：

- `[ ]` **红光护眼模式 (Night Vision Mode)**：一键切换全屏幕配色为红黑风格，保护夜间户外天文学观测的暗适应。
- `[x]` **视星等（亮度）动态预测**：基于相位角和距离计算卫星视星等，帮助判断是否超过肉眼可视极限。
- `[ ]` **星链列车追踪 (Starlink Train)**：动态追踪并展示最近发射批次的星链列车（Starlink Trains）的壮观过境。
- `[ ]` **过境倒计时与蜂鸣器提醒**：可为即将到来的最佳观测窗口设置闹钟，AOS 前由设备扬声器/蜂鸣器发出声音提醒。
- `[ ]` **AR 寻星雷达 / 罗盘指向**：利用内置 6 轴 IMU，在屏幕中心显示准星，引导用户将设备对准空中正在过境的卫星位置。
- `[ ]` **极度省电与定时唤醒 (Deep Sleep)**：计算出下一次过境的准确时间后，让 ESP32 进入深度睡眠模式，并在过境前几分钟通过 RTC 定时器自动唤醒亮屏。
- `[ ]` **业余无线电频率展示**：在推荐和详情页增加国际空间站（ISS）或气象卫星的下行通信频率（如 APRS / SSTV），方便 HAM 用户进行通联。
- `[ ]` **背景亮星 / 星座参考系**：在 3D 和 2D 视图中渲染几颗极其明亮的导航星体（如天狼星），为用户的观测提供物理位置参照。
- `[ ]` **局域网 WebServer 导出**：建立轻量级本地 Web 服务器，允许用户使用手机通过 WiFi 访问，查看更详细的 24 小时过境日程表。

## 许可证

本项目采用 [PolyForm Noncommercial License 1.0.0](LICENSE) 许可证。

**该许可证允许个人使用、科研和教育等非商业用途，但严禁任何形式的商业行为。**